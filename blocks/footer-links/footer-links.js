export default async function decorate(block) {
  // The footer-links block contains a single row with 5 cells (one per column)
  // Each cell has an h2 heading and a ul list of links
  // No DOM restructuring needed — CSS handles the grid layout
}
