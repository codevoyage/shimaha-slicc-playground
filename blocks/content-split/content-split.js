export default async function decorate(block) {
  // Content-split block: image cell + text cell in a single row
  // The EDS framework has already created the row/cell structure
  // No additional DOM manipulation needed for the basic layout
}
